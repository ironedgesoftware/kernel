import:
  services:
    file: services.yml
  routes:
    file: routes.yml

users:
  guest:
    username: guest
    password: guesttest

components:
  fantasy_vendor/fantasy_component_1:
    registered_param: registered_value_1

  ironedge/kernel:
    config:
      processor:
        class: \IronEdge\Component\Kernel\Test\Helper\ConfigProcessor2
        priority: 2