import:
  services:
    file: services.yml
  routes:
    file: routes.yml

users:
  guest:
    username: guest
    password: guesttest

ironedge/kernel:
  config:
    processorClass: \IronEdge\Component\Kernel\Test\Helper\ConfigProcessor2